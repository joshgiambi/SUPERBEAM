# Environment Configuration Template
# Copy this file to .env and customize for your local development

# === DATABASE CONFIGURATION ===
# For local PostgreSQL (recommended for development):
DATABASE_URL="postgresql://your_username@localhost:5432/converge_viewer"

# For Neon cloud database (original setup):
# DATABASE_URL="postgresql://[user[:password]@][netloc][:port][/dbname][?param1=value1&...]"

# Force local PostgreSQL mode (optional):
# USE_LOCAL_POSTGRES=true

# === SERVER CONFIGURATION ===
NODE_ENV=development
PORT=3000

# === OPTIONAL SETTINGS ===
DEBUG=true

# === SEGVOL AI PREDICTION (OPTIONAL) ===
# Enable AI-powered slice prediction with SegVol model
# See SEGVOL_SETUP.md for complete setup instructions
SEGVOL_SERVICE_URL=http://127.0.0.1:5001
SEGVOL_TIMEOUT=30000

# === MEM3D AI PREDICTION (OPTIONAL - RECOMMENDED) ===
# Enable memory-augmented AI prediction with Mem3D model
# Mem3D is faster (~200ms) and better suited for interactive slice-by-slice workflow
# See MEM3D_SETUP.md for complete setup instructions
MEM3D_SERVICE_URL=http://127.0.0.1:5002
MEM3D_TIMEOUT=30000

# === NNINTERACTIVE AI TUMOR SEGMENTATION (OPTIONAL) ===
# Enable interactive 3D tumor segmentation with nnInteractive
# Draw scribbles on 3-5 key slices → AI generates full 3D volume
# See NNINTERACTIVE_SETUP.md for complete setup instructions
NNINTERACTIVE_SERVICE_URL=http://127.0.0.1:5003
NNINTERACTIVE_TIMEOUT=60000

# === INSTRUCTIONS ===
# 1. Copy this file: cp .env.template .env
# 2. Edit .env with your local settings
# 3. Never commit .env to Git (it's in .gitignore)
# 4. For AI prediction setup, see MEM3D_SETUP.md or SEGVOL_SETUP.md
